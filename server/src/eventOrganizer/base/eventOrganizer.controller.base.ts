/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { EventOrganizerService } from "../eventOrganizer.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EventOrganizerCreateInput } from "./EventOrganizerCreateInput";
import { EventOrganizerWhereInput } from "./EventOrganizerWhereInput";
import { EventOrganizerWhereUniqueInput } from "./EventOrganizerWhereUniqueInput";
import { EventOrganizerFindManyArgs } from "./EventOrganizerFindManyArgs";
import { EventOrganizerUpdateInput } from "./EventOrganizerUpdateInput";
import { EventOrganizer } from "./EventOrganizer";
import { TicketFindManyArgs } from "../../ticket/base/TicketFindManyArgs";
import { Ticket } from "../../ticket/base/Ticket";
import { TicketWhereUniqueInput } from "../../ticket/base/TicketWhereUniqueInput";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class EventOrganizerControllerBase {
  constructor(
    protected readonly service: EventOrganizerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: EventOrganizer })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: EventOrganizerCreateInput
  ): Promise<EventOrganizer> {
    return await this.service.create({
      data: data,
      select: {
        bankAccount: true,
        createdAt: true,
        firstname: true,
        id: true,
        lastname: true,
        phoneNumber: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [EventOrganizer] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(EventOrganizerFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<EventOrganizer[]> {
    const args = plainToClass(EventOrganizerFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        bankAccount: true,
        createdAt: true,
        firstname: true,
        id: true,
        lastname: true,
        phoneNumber: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: EventOrganizer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: EventOrganizerWhereUniqueInput
  ): Promise<EventOrganizer | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        bankAccount: true,
        createdAt: true,
        firstname: true,
        id: true,
        lastname: true,
        phoneNumber: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: EventOrganizer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: EventOrganizerWhereUniqueInput,
    @common.Body() data: EventOrganizerUpdateInput
  ): Promise<EventOrganizer | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          bankAccount: true,
          createdAt: true,
          firstname: true,
          id: true,
          lastname: true,
          phoneNumber: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: EventOrganizer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: EventOrganizerWhereUniqueInput
  ): Promise<EventOrganizer | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          bankAccount: true,
          createdAt: true,
          firstname: true,
          id: true,
          lastname: true,
          phoneNumber: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Ticket",
    action: "read",
    possession: "any",
  })
  @common.Get("/:id/tickets")
  @ApiNestedQuery(TicketFindManyArgs)
  async findManyTickets(
    @common.Req() request: Request,
    @common.Param() params: EventOrganizerWhereUniqueInput
  ): Promise<Ticket[]> {
    const query = plainToClass(TicketFindManyArgs, request.query);
    const results = await this.service.findTickets(params.id, {
      ...query,
      select: {
        createdAt: true,

        eventId: {
          select: {
            id: true,
          },
        },

        id: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "update",
    possession: "any",
  })
  @common.Post("/:id/tickets")
  async connectTickets(
    @common.Param() params: EventOrganizerWhereUniqueInput,
    @common.Body() body: TicketWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tickets: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id/tickets")
  async updateTickets(
    @common.Param() params: EventOrganizerWhereUniqueInput,
    @common.Body() body: TicketWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tickets: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @nestAccessControl.UseRoles({
    resource: "EventOrganizer",
    action: "update",
    possession: "any",
  })
  @common.Delete("/:id/tickets")
  async disconnectTickets(
    @common.Param() params: EventOrganizerWhereUniqueInput,
    @common.Body() body: TicketWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tickets: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
